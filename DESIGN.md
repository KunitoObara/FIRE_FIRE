# DESIGN.md

このファイルは、フロントエンド(`src/frontend`)のデザインシステム・UI実装方針を定義する。`docs/`配下の要件定義書(機能要件)に対して、本ファイルは「どう見せるか・どう実装するか」を扱う。

## 1. デザイン原則

- **アダプティブデザイン**: PC/タブレット/スマートフォンでの利用を想定する(docs/fire-asset-management-requirements.md 5章)。画面幅ごとに専用レイアウトを作るのではなく、Tailwindのレスポンシブユーティリティで同一コンポーネントを可変させることを基本とする。
- **金融データの視認性を優先する**: 個人の資産・収支を扱うため、桁の読み違い・誤操作を避けることを他の装飾より優先する。金額は3桁区切り・通貨単位を明示し、削除等の破壊的操作(資産分類削除、2FA再設定など)には確認導線を挟む。**これは演出を一律に禁じる原則ではない** — 数字を読み取る妨げにならない範囲であれば動きを使ってよく、グラフの登場アニメーションは9章の条件付きで採用する。「視認性優先」だけを根拠にアニメーションを止めない。
- **マスタデータ由来の項目をコードにハードコードしない**: 資産分類軸はユーザーが追加・編集できるマスタデータである(4.3)。UI側(色・アイコン・レイアウト)もこの前提に合わせ、固定件数を前提にしたレイアウトを組まない。[category-master-extensibility-check](.claude/skills/category-master-extensibility-check/SKILL.md)スキルと対応する方針。

## 2. 技術スタック

| 用途 | 選定 | 備考 |
|---|---|---|
| CSSフレームワーク | Tailwind CSS | 指定 |
| UIコンポーネント | shadcn/ui | 指定。CLI経由で必要なコンポーネントのみ`src/frontend`に追加する(フルインストールしない) |
| アイコン | lucide-react(既定) + FontAwesome | lucide-reactはshadcn/uiの標準アイコンセットで、既定はこちらを使う。FontAwesome(`@fortawesome/react-fontawesome` + `free-solid-svg-icons`)は指定により併用する。**同じ用途のアイコンで2つを混ぜない**(例: パスワード表示トグルは全画面でFontAwesomeの`eye`/`eye-slash`)。1画面に両方が現れること自体は許容する — shadcn/uiのコンポーネントが内部でlucideを使っており(`ui/checkbox.tsx`の`CheckIcon`等)、画面単位で片方に統一することは構造上できないため |
| フォーム/バリデーション | react-hook-form + zod | shadcn/uiのForm系コンポーネントが前提とする組み合わせ |
| チャート | Recharts(shadcn/uiの`chart`コンポーネント経由) | 資産推移(折れ線)・分類別内訳(円グラフ)・FIRE達成度ゲージ(RadialBarChartで代用)に使用 |
| データテーブル | @tanstack/react-table(shadcn/uiの`data-table`パターン) | 収支明細一覧(B3)の検索・並び替えに使用 |
| 6桁確認コード入力 | shadcn/ui `input-otp` | 2FA登録(A3)・2FA検証(A5)で使用 |
| QRコード表示 | react-qr-code 等の軽量ライブラリ | 2FA登録(A3)・2FA再設定(B10経由)で使用 |
| 日付処理 | date-fns | FIRE到達予測日の計算・表示、期間絞り込み(B1/B3)に使用 |
| CSVパース | papaparse | B2のマネーフォワードCSV取込に使用。引用符で囲まれたフィールド・改行コードの揺れ・末尾の空行を仕様どおりに扱う。実ファイルは全フィールドがダブルクォート囲みで、入出金明細(Phase 2)は摘要にカンマを含むため、自前の分割では取りこぼす。文字コード判定(Shift_JIS/UTF-8)はライブラリに任せず`src/lib/csv/decode.ts`で行う |
| 通知・完了メッセージ | shadcn/ui `sonner` | CSV取込完了・保存完了などの一過性メッセージに使用 |
| 日本語フォント | next/font 経由でNoto Sans JP等を最適化配信 | 全画面日本語UIのため |

各追加ライブラリは特定の画面要件に対応づけて選定している(3章参照)。要件にない機能のために先回りして追加しない。

## 3. カラーシステム

- shadcn/ui標準のCSS変数ベースのトークン(`--background`, `--foreground`, `--primary`など)をそのまま採用し、ライト/ダーク両方に対応できる形で変数を定義する。ダークモードの実装要否自体は8章のオープン課題とする。
- **資産分類カラー**: 分類はマスタデータとして動的に増減するため、分類名やIDに対して色を決め打ちでマッピングしない。**分類マスタ(B4)の登録順のインデックスを、8色の質的パレット(`--chart-1`〜`--chart-8`)のスロットにそのまま対応させる。** 分類が増減しても既存の分類の色は変わらず、分類軸や表示期間を切り替えても同じ分類は同じ色で描かれる(実装: `src/frontend/src/lib/dashboard/category-color.ts`)。
  - **金額順・表示順に色を振らない**。その時の並びに色を割り当てると、順位が入れ替わるたびに同じ分類の色が変わり、グラフを見比べられなくなる。色は「順位」ではなく「分類そのもの」に紐づける
  - **スロット数を超えた分は色を作って回さず、「その他」にまとめる**。パレットは隣り合うスロットの識別性を検証したうえで並び順を決めているため、色を機械的に増やすと識別できない色同士が並ぶ。描画順もスロット順に固定する(検証していない組み合わせが隣接しないようにするため)
  - ハッシュ値によるローテーション方式は採らない。ハッシュは衝突しうるため、隣り合う2分類に同じ色が当たる可能性を排除できない
- **負債の色(B1・B11)**: 負債は資産分類カラーのスロット(`--chart-1`〜`--chart-8`)を使わず、**負債専用の固定色**(`--destructive`系)を1つ割り当てる。理由は2つあり、いずれも分類の色を動的に配る仕組みとは別の話である。
  - 分類別内訳の円グラフで負債がスロットを1つ占めると、実際に保有している資産の分類が「その他」へ押し出される。負債は分類軸ごとに1スライスに固定なので、スロットを配る対象にする必要がない
  - 資産と負債は符号が逆であり、同じ質的パレットに並べると負債が資産の一種に見える。色で符号の違いを示す
  - **`--destructive`は削除ボタン(`btn-destructive`)・入力エラー(`input-error` / `field-error`)にも使っている色である。** 負債の金額に同じ色を当てるのは意図的で、赤が負の値を表す会計上の慣用に沿うため。ただし単独の赤い数字が警告に読まれないよう、**負債の金額には必ずラベル(「負債合計」「負債」など)を添える**。逆に、負債の金額の近くにエラー表示を置くときは文言で区別が付くようにする(B11は負債合計と入力エラーが同じ画面に出る)
  - **色だけに頼らず、負債のスライスにはハッチング(斜線)を重ねる。** `--destructive`(赤)と1色目の`--chart-1`(赤みのあるオレンジ)はどちらも暖色で、円グラフでは負債が最後のスライスになるため12時の位置で必ず`--chart-1`と隣り合う。凡例の色見本もグラフと同じハッチングにする(見本とスライスの見た目が違うと対応が取れない)。色以外の手がかりを併用する方針はリスクレベル(下記)と同じ
- **リスクレベル(B9)**: 赤/緑の二値だけに頼らず、段階的なグラデーション+アイコン(形状)を併用し、色覚特性に依らず区別できるようにする。
- 実際にチャートを実装する際は、パレットの具体的な数値やコントラスト基準についてこの環境で利用可能な`dataviz`スキルを参照する(質的/連続的パレットの選び方とアクセシビリティ検証済みの手法がまとまっている)。DESIGN.md側に重複した色定義を持たせない。

## 4. タイポグラフィ

- UIは全て日本語。可読性を優先し、和文フォント(Noto Sans JP等)+ 数字部分はTailwindの`tabular-nums`で桁を揃える(金額・日付が縦に並ぶダッシュボード・一覧画面で特に重要)。
- 見出し/本文/補助テキストのスケールはTailwindのデフォルトタイプスケールをベースにし、独自スケールを新たに定義しない。

## 5. レイアウトパターン

画面一覧(docs/screen-list-and-transitions.md)のとおり、大きく2系統のレイアウトに分かれる。

- **認証系(A1〜A8)**: 共通ヘッダー/サイドバーなし、中央寄せの1カラムカードレイアウト。未ログイン状態専用のシンプルなシェルを使う。
- **ログイン後(B1〜B11)**: 共通ヘッダー+サイドバーによる「ダッシュボードアプリ型」シェル(docs/screen-list-and-transitions.md 1章)。相互に自由遷移できる主要画面はサイドバーから常時アクセス可能にする。スマホ幅ではサイドバーを折りたたみ/ハンバーガーメニュー等に切り替える。
  - ヘッダー右端のアバターは**ユーザーメニュー**の起点とし、B10へ直接リンクさせない。メニューにログイン中のメールアドレス・「アカウント設定」(B10)・「ログアウト」を収める(docs/screen-requirements-account.md 2章)。サイドバーは相互に自由遷移できる主要画面(B1〜B11)を並べる場所とし、シェルの外へ抜けるログアウトは混ぜない。
- ブレークポイントはTailwind標準(`sm`/`md`/`lg`)をPC/タブレット/スマートフォンの目安として使い、独自のブレークポイントは必要になった時にのみ追加する。

## 6. コンポーネント実装の指針

- **インラインバリデーション**: サインアップ(A1)・パスワード再設定(A7)・不動産登録(B7)・FIRE目標設定(B8)・想定利回り設定(B9)など、要件定義書が明示的に「インラインエラー表示」「リアルタイムバリデーション」を求めている画面では、react-hook-form + zodのフォーム単位でエラーメッセージを表示し、送信後にまとめて出す形にしない。
- **タブ切替で入力値を保持する**: B8(目標設定方式)・B2(CSV取込種別)のようにタブ切替がある画面は、非表示タブの入力値を破棄しない(要件定義書に明記された挙動)。
- **一覧+インライン編集**: B9(想定利回り・リスク設定)は一覧内でのインライン編集を要求している。別画面への遷移を挟まない。
- **行を増減できるフォーム**: B11(負債入力)は1画面の中でフォームを追加・削除し、まとめて保存する。react-hook-formの`useFieldArray`で1つのフォームとして扱い、行ごとに独立したフォーム状態を持たせない。追加・削除は保存するまで画面上の状態に留め、行単位で保存しない(docs/screen-requirements-dashboard.md B11)。行の`key`に配列インデックスを使わない — 行を削除したときに残った行の入力値が1つずつずれる。
- **削除のブロッキング確認**: B4(資産分類マスタ)で「既存データが紐づく分類は削除禁止」という要件があるため、削除操作は`alert-dialog`等で確認し、禁止時はダイアログ内で理由を明示する。
- **実行前の内容確認**: A1(サインアップ)のように、後戻りしにくい登録操作の直前に入力内容を確認させる画面では`alert-dialog`を使う。確認ダイアログはバリデーション通過後にのみ開き、パスワード等の秘匿値は伏字で表示する。
- **ログアウトの確認**: ログアウトはデータを壊す操作ではないが、2FAが必須で再ログインに認証アプリの確認コードが要るため、`alert-dialog`で確認を挟む(docs/screen-requirements-account.md 2章)。共通ヘッダーのユーザーメニューとA2・A3の「別のアカウントでログイン」で**同じコンポーネントを使い**、見出し・ボタン・実行内容は置き場所ごとに変えない。**注記の文言だけ**は2FAの登録状況に応じて出し分ける(2FA未登録のA2・A3で「次回は確認コードが必要」と出すと事実と食い違うため)。出し分けの内容はaccount.md 2章の表を正とし、画面ごとに文言を作り足さない。
- **パスワード入力欄の表示トグル**: パスワードを入力する欄(A1・A4・A7・A8・B10)は入力欄右端に目のアイコンを置き、平文表示を切り替えられるようにする。複数のパスワード欄がある画面では欄ごとに独立して切り替える。
- **ソーシャルログインの導線(A1・A4)**: メール/パスワードのフォームの下に「または」の区切り線を挟み、その下にプロバイダボタンを置く。ボタンはアウトライン(副次)スタイルとし、主導線であるメール/パスワードより目立たせない。プロバイダのアイコンは各社のブランドマーク(Googleは4色のG)をそのまま使い、lucide/FontAwesomeのアイコンで代用しない — ブランドガイドラインの要求であり、ユーザーがどの認証先かを一目で判別できるようにするため。
- **参照専用画面**: B3(収支明細一覧)・B6(不動産詳細)は編集機能を持たない参照専用画面である。編集導線を誤って追加しない。
- **グラフの登場アニメーション**: B1の3つのグラフには表示アニメーションを入れる。対象・時間・停止条件は9章にまとめる。

## 7. 提案した追加ライブラリの根拠(画面ID対応表)

| ライブラリ | 対応する画面ID | 理由 |
|---|---|---|
| Recharts(shadcn chart) | B1 | 資産推移グラフ・分類別内訳円グラフ・FIRE達成度ゲージ |
| @tanstack/react-table | B3 | 絞り込み・並び替え可能な取引一覧 |
| input-otp | A3, A5 | 6桁確認コード入力 |
| react-qr-code | A3, B10 | 2FA登録・再設定時のQRコード表示 |
| date-fns | B1, B3, B8 | 期間絞り込み、到達予測日計算 |
| papaparse | B2 | マネーフォワードCSVのパース(引用符・改行の揺れへの対応) |
| sonner | B1, B2, B7, B8, B9, B11 | 取込完了・保存完了等の完了通知、B1のGoogleアカウント連携の失敗通知 |
| react-hook-form + zod | A1, A7, B7, B8, B9, B11 | インラインバリデーションが明示要求されている画面群。B11は`useFieldArray`で行の増減も扱う |
| alert-dialog | A1, A2, A3, B4, B10, B11, 共通ヘッダー | 登録前の入力内容確認・削除前のブロッキング確認・B11の保存時の削除確認・B10の2FA再設定/連携解除の確認・ログアウトの確認(共通ヘッダーのユーザーメニューとA2/A3の「別のアカウントでログイン」) |
| dropdown-menu(shadcn) | 共通ヘッダー(B1〜B11) | ヘッダーのユーザーメニュー。メールアドレスの表示・アカウント設定(B10)・ログアウトを1つのメニューに収める。キーボード操作とフォーカストラップを自前で書かずに済ませるため、Radixベースの`dropdown-menu`を使う |
| FontAwesome | A1, A4, A7, A8, B10 | パスワード表示/非表示トグルの目のアイコン(`eye` / `eye-slash`)。対象画面は6章の同トグルの記述と揃える |

## 8. 今後の検討事項(オープン課題)

本章は未決事項だけを並べる。**続く9章はオープン課題ではなく確定した仕様である。** 既存のコードが「DESIGN.md N章」で各章を参照しているため章番号を繰り上げられず、決定済みの章が本章の後ろに来る並びになっている。

- ダークモード対応の要否(shadcn/ui標準のCSS変数構成にしておけば後からの対応は容易)
- ブランドカラー・ロゴ等のビジュアルアイデンティティは未定(現状はshadcn/uiのデフォルトテーマをベースにする)
- チャートライブラリはRecharts(shadcn連携)を提案しているが最終確定ではない
- レスポンシブブレークポイントの具体的な境界値は実装時に画面を見ながら調整する

## 9. モーション(グラフの表示アニメーション)

B1のグラフには登場アニメーションを入れる。完成形が静止したまま現れるより、値が伸びていく過程を見せた方が「いま読み込まれた数字」であることが伝わるため。ただし金額の読み取りを妨げない範囲に限る(1章)。

### 対象と動き

| 対象 | 動き |
|---|---|
| 資産推移グラフ(折れ線・面) | 左から右へ描き出す |
| 分類別内訳(円グラフ) | 12時の位置から時計回りにスイープする |
| FIRE達成度ゲージ | 0%から達成率までリングを伸ばし、中央の%も同じ進み方でカウントアップする |

- **ゲージ中央の%はリングと同期させる**。数値だけ最終値で固定してリングが後から追いつく形にすると、同じ瞬間に2つの違う達成率が画面に出る。逆にリングだけ動かして数値を止めるのも同じ理由で採らない
- 同期は**同じ進捗値から両方を描く**ことで担保する。リングをライブラリのアニメーション、数値を自前のカウントアップ、と別々の仕組みに分けない。同じ600msでもイージングの実装が違えば途中の進み方がずれ、リングと数値が食い違う
- **%以外の数値はアニメーションさせない**。目標資産額・現在資産額・内訳の構成比といった金額や比率は最初から最終値で出す。桁の多い金額が途中の値で読めてしまうと、桁の読み違いをこちらから作ることになる(1章)。動かしてよいのは、リングという図形と一体で読まれるゲージ中央の%だけ
- 負債のスライス(3章)も資産のスライスと同じ1周のスイープに含める。負債だけ別に動かすと、円がつながって見えなくなる
- ツールチップ・凡例・空状態(データが無いときの案内)は対象外
- **円グラフは`paddingAngle`と登場アニメーションを併用できない。** 併用するとRechartsが角度を広げきれず、開始フレーム(ほぼ0度の扇形)のまま止まって円が出ない(`CategoryBreakdownChart.tsx`にこの理由でアニメーションを切ったコメントが残っている)。`paddingAngle`は隣り合う色の境界を見せるためのもの(3章)で外せないので、スイープはRechartsのアニメーションに任せず、描画後に自前で角度を広げるなど別の手段で実現する

### 時間と再生条件

- 再生時間は**600ms前後**、イージングは`ease-out`。ログイン直後の最初の画面なので、資産状況の把握を待たせない長さに収める
- 再生するのは**そのグラフ自身のデータが変わったとき**だけとする。効くトリガーはグラフによって違う

| 対象 | 再生するとき |
|---|---|
| 資産推移グラフ・分類別内訳 | 初回描画時、分類軸切替・表示期間切替でデータが差し替わったとき(切り替えた結果が変わったことを見せるため) |
| FIRE達成度ゲージ | 初回描画時のみ。**分類軸切替・表示期間切替では再生しない** — ゲージはB1のセレクタに追従しないため(docs/screen-requirements-dashboard.md B1)、切り替えても値が変わらない |

- 次の場合は再生しない。ユーザーの操作と対応しないタイミングで動くと、画面が落ち着かないだけになる
  - ツールチップ・凡例へのホバー
  - ウィンドウのリサイズ、スクロール
  - データが変わらないまま再レンダリングが起きたとき
- ローディングのスケルトンからグラフへの差し替えは即座に行い、スケルトンのパルスとグラフのアニメーションを重ねない

### `prefers-reduced-motion`

OS側で視差効果を減らす設定(`prefers-reduced-motion: reduce`)になっている場合は、**アニメーションを行わず最終状態を即座に描く**。Rechartsは`isAnimationActive`で切り替えられるので、この判定は`src/hooks/`に共通のhookを1つ置いて各チャートに配り、チャートごとに`matchMedia`を書かない(`use-mobile.ts`と同じ形)。

判定は「取得できなければアニメーションしない」側に倒す。自動テスト(jsdom)には`matchMedia`が無く、この既定によりテストは最終状態だけを検証できる。アニメーションの有無で要素の出現タイミングが変わらないため、既存のB1のテストに待機を足す必要もない。

### 既存コードとの関係

現状のB1は3つのチャートとも`isAnimationActive={false}`でアニメーションを止めており、その根拠として「DESIGN.md 1章」を挙げたコメントが付いている(`FireProgressGauge.tsx` / `CategoryBreakdownChart.tsx` / `NetWorthTrendChart.tsx`)。本章の追加でその根拠は成立しなくなるので、値を変えるときはコメントも本章を指すように直す。値だけ変えるとコードとコメントが食い違ったまま残る。
